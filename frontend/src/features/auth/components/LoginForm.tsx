import {useForm} from 'react-hook-form';import {zodResolver} from '@hookform/resolvers/zod';import {loginSchema,LoginFormData} from '../validators/login.schema';import {useLogin} from '../hooks/useLogin';export default function LoginForm(){const {onSubmit}=useLogin();const {register,handleSubmit}=useForm<LoginFormData>({resolver:zodResolver(loginSchema),defaultValues:{rememberMe:false}});return <form onSubmit={handleSubmit(onSubmit)} className='space-y-4'><input {...register('username')} placeholder='Username' className='w-full border p-2 rounded'/><input type='password' {...register('password')} placeholder='Password' className='w-full border p-2 rounded'/><label><input type='checkbox' {...register('rememberMe')}/> Remember Me</label><button className='w-full bg-blue-600 text-white p-2 rounded'>Login</button><p>Mock: admin/admin123</p></form>}
